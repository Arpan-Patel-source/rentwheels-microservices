spring.application.name=feedback-service
server.port=8087

# Each microservice owns its own database (created automatically if missing)
spring.datasource.url=jdbc:mysql://localhost:3306/feedback_db?createDatabaseIfNotExist=true
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.jpa.hibernate.ddl-auto=update

# Register with eureka-server
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
